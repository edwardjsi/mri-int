name: FINAL FIX
on:
  workflow_dispatch:
  schedule:
    # 4:00 PM IST / 10:30 AM UTC, Monday–Friday
    - cron: "30 10 * * 1-5"
jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install Dependencies
        run: pip install -r requirements.txt
      - name: Show revision and pipeline script
        run: |
          git rev-parse HEAD
          sed -n '1,80p' scripts/pipeline_cloud.sh
      - name: RUN REAL PIPELINE (ENGINE_CORE)
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          DB_SSL: "true"
          PYTHONPATH: ${{ github.workspace }}
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          AWS_REGION: ${{ secrets.AWS_REGION }}
          SES_REGION: ${{ secrets.SES_REGION }}
          SES_SENDER_EMAIL: ${{ secrets.SES_SENDER_EMAIL }}
        run: bash scripts/pipeline_cloud.sh
      - name: DB Freshness Check
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
        run: python scripts/db_freshness_check.py
