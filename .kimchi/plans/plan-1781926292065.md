I'm in plan mode, so I'll lay out a precise Phase D1 plan with one important architectural flag the user needs to weigh in on.

## Plan — Phase D1: Extend QIF agents to persist per-quarter/year underlying metrics

### ⚠️ Architectural flag — **annual vs quarterly data source**

The doc's JSONB shape has `"year": 2026, "quarter": 1` — implying quarterly. But on exploration, the agents actually read **`fundamental_financials`** (annual, year-only), not `aae_quarterly_financials`. Two paths:

| Path | What changes | Risk |
|---|---|---|
| **X — Per-year detail from annual source** (recommended for D1) | Keep `pipeline.get_financial_history()` reading `fundamental_financials`. JSONB shape becomes `by_year[]` keyed on `year` only. Trajectory: "ROCE was 13% a year ago, 14.5% two years ago." | Minimal — no data-source switch, no field gap. Loses intra-year resolution. |
| **Y — Switch to quarterly source** (matches doc literally) | `get_financial_history()` reads `aae_quarterly_financials`. Required: handle missing `capital_employed` (compute from `total_assets - current_liabilities`), handle quarterly collection gaps, EPS for translation agent. JSONB shape stays `by_quarter[]`. | Higher risk — `aae_quarterly_financials` may have gaps for older periods, agents need field-mapping, scores may shift because of seasonality. |

**My recommendation:** **Path X for D1**, then queue Path Y as a Phase D1.5 (a few days later) once we've validated per-year detail is shipped + tested. Per-year trajectory is already a 5× richness improvement over today's "ROCE < WACC flag." Quarterly can land safely after.

### Phase D1 — exact changes (Path X)

**File 1 — `migrations/005_qif_agent_details.sql`** (NEW, ~30 lines)
- Header comment matching 004_debate_cache.sql pattern (date, scope, idempotency, companion files)
- `ALTER TABLE public.quality_verdicts ADD COLUMN IF NOT EXISTS agent_details JSONB DEFAULT '{}'::jsonb;`
- Optional GIN index on `agent_details` if queryable

**File 2 — `api/schema.py`** (1 line addition, around line 309)
- Append the same `ALTER TABLE ... ADD COLUMN IF NOT EXISTS agent_details JSONB DEFAULT '{}'::jsonb;` after the existing `quality_verdicts` ALTER lines, so fresh deploys auto-create the column without needing the migration file

**File 3 — `engine_fundamental/agents.py`** (rewrite, ~270 lines)
- Keep all 7 agent function signatures backward-compatible: return `{"score", "reason", "confidence", "detail": {...}}`
- Add a new helper `get_financial_history_typed(symbol)` that returns ordered list of `{"year", "revenue", "ebitda", ...}` dicts (currently returns tuples — need to change callers too)
- Each agent extended to compute **per-year** detail alongside the score:
  - `revenue_quality_agent` detail: `{"growth_yoy_pct", "growth_3y_avg_pct", "trend"}` per year
  - `margin_quality_agent` detail: `{"opm_pct", "opm_3y_avg_pct", "compression_bps_yoy"}` per year
  - `operating_leverage_agent` detail: `{"ebitda_to_revenue_growth_ratio"}` per year
  - `working_capital_agent` detail: `{"wc_days_current", "receivable_growth_yoy_pct"}` per year
  - `capital_efficiency_agent` detail: `{"roce_pct", "wacc_pct", "gap_pct"}` per year
  - `business_evolution_agent` detail: `{"asset_growth_pct", "margin_change_3y_bps"}` per year
  - `financial_translation_agent` detail: `{"cash_conversion_ratio"}` per year

**File 4 — `engine_fundamental/pipeline.py`** (~40 lines added)
- New helper `_build_agent_details_json(agents_results)`:
  - Iterates the 7 agents' `detail` arrays
  - Merges into a single `by_year[]` array — one entry per fiscal year, each entry contains all 7 agents' per-year detail fields + their scores for that year
  - Computes `trajectory` summary: `{score_trend, score_change_yoy, roce_change_yoy_bps, margin_compression_bps_yoy, revenue_cagr_3y_pct, quarters_observed}`
- Update `run_quality_pipeline()`:
  - Switch `cur.execute("SELECT year, revenue, ... FROM fundamental_financials WHERE symbol = %s")` to `RealDictCursor` so rows come back as dicts (currently tuples — need to update downstream consumers)
  - Pass the new `agent_details` JSONB to the `INSERT ... ON CONFLICT DO UPDATE` statement
- Backward compat: existing 7-agent callers (`api/fundamental.py`, `engine_perx/orchestrator.py`, `engine_core/orchestrator.py`, `scripts/pipeline_cloud.sh`) continue to work because the agent return shape gains a `detail` field, doesn't lose any existing fields

**File 5 — `engine_fundamental/test_qif_agent_details.py`** (NEW, ~250 lines)
- Pattern from existing `test_*_credibility.py` files:
  - Uses live Neon DB (per Decision 027 RDS protection rules + established test pattern)
  - Uses disposable `_QIF_DETAILS_<uuid>` symbols
  - Full cleanup of inserted rows in `quality_verdicts` and `fundamental_financials`
- Test cases (~10):
  - Each of 7 agents returns a `detail` key with the documented per-year shape
  - 2-year financials → `trajectory.score_trend` correctly classified (improving/stable/declining)
  - 4-year financials → `revenue_cagr_3y_pct` computed correctly
  - Empty `agent_details` fallback works when `fundamental_financials` is empty
  - `pipeline.run_quality_pipeline()` writes `agent_details` JSONB that round-trips through Postgres
  - Trajectory summary handles edge cases: 1 year, 2 years, 5+ years, declining YoY, accelerating decline

### Verification (Phase D1 done criteria)

1. ✅ `pytest engine_fundamental/test_qif_agent_details.py` — all tests pass against live Neon DB
2. ✅ Existing tests (`test_debate_management_integrity`, `test_graveyard_credibility`, `test_master_score_credibility_weight`, `test_narrative_credibility_context`) — all still pass (109 + 10 new = 119 total)
3. ✅ Smoke run on POLYCAB, KirlosEngine, QPOWER: `run_quality_pipeline("POLYCAB")` writes a non-empty `agent_details` JSONB with `by_year[]` length matching `fundamental_financials` row count + a populated `trajectory` summary
4. ✅ `SELECT agent_details FROM quality_verdicts WHERE symbol='POLYCAB'` returns valid JSONB with the documented shape
5. ✅ `python -c "from engine_fundamental.agents import revenue_quality_agent; print(revenue_quality_agent([...]))"` — old callers still work (added `detail` key, didn't remove `score`/`reason`/`confidence`)
6. ✅ Committed to `feature/data-richness` branch (local only, no push)

### Out of scope (deferred)

- Updating context builders in `engine_debate/context_pe_expansion.py` — that's Phase D2
- Re-running QIF for the 63 already-covered stocks — that's Phase D3
- Backfilling AAE + QIF for the 109 uncovered stocks — that's Phases A1–A5
- Quarterly data source switch — that's Phase D1.5 (proposed, not in current sprint)
- Force-rerun-all-debates pass — that's Phase A5
- NSE/BSE retry fallback for unfillable stocks — that's Phase A2/A3

### Time & cost estimate

| Sub-step | Time | Cost |
|---|---|---|
| Migration file | 10 min | $0 |
| Schema.py ALTER | 5 min | $0 |
| Agents.py rewrite | 60 min | $0 |
| Pipeline.py helper + integration | 30 min | $0 |
| Tests (10 cases) | 45 min | $0 (live Neon DB) |
| Smoke runs (3 symbols) | 15 min | $0 |
| **Phase D1 total** | **~2.5 hrs** | **$0** |

### What I need from you before code

Two questions:

1. **Path X (annual, per-year) vs Path Y (quarterly)?** Recommendation: X for D1, Y as separate Phase D1.5.
2. **Stay on `main` plan, or adjust commit cadence?** Doc says "4-5 commits on `feature/data-richness`, single PR to main." I'd do 4 commits for D1 alone (migration, agents, pipeline, tests) which is heavier than the doc's plan — but each commit is independently verifiable per project convention.

Once you answer those, I'll execute (assuming you switch me out of plan mode, or mark the plan complete with `<done>` for me to start).

<done>