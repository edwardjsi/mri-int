Here's the plan. Single file, single chunk, verifiable.

## Goal
Create `/home/immanuels/Desktop/mri-int/docs/investor/explain-investor.md` containing a complete, code-grounded explanation of every module in the MRI platform for an educated investor audience, written in plain English with technical honesty about both strengths and limitations.

## Constraints
- **Audience:** educated investor (financially literate, not necessarily technical) — every claim must be backed by either the code, the README, or the backtest report
- **Length:** substantive but skimmable — target ~2,500–3,500 words with clear section headers
- **Tone:** honest, not promotional — explicitly call out where the README is stale (e.g., the actual scoring weights differ from the documented ones) and where layers are aspirational vs validated
- **No fabricated claims:** every figure (CAGR, drawdown, Sharpe, threshold values) traces to a file I read
- **One file, no new dependencies** — pure Markdown

## Chunks

### Chunk 1: Create the file
- **Scope:** `docs/investor/explain-investor.md` (single file)
- **Depends On:** nothing
- **Accept When:**
  1. File exists at the specified path
  2. File is valid Markdown and renders cleanly
  3. Contains all sections listed below
  4. Every quantitative claim cites its source (file + line number)
- **Open Questions:** none — user already chose location

### Planned file structure

```markdown
# MRI Platform — Investor's Explanation

> A plain-English walkthrough of every engine in the Market Regime
> Intelligence platform: what each one does, how it works mechanically,
> what data it uses, and where to be skeptical.

## What This Repo Is
(one-paragraph summary)

## The Big Picture: How The Layers Fit Together
(ASCII / table showing data → regime → score → signals → debate → PERX)

## Headline Track Record
(backtest results table — 17-year, T+1, survivorship-bias-corrected)
+ Caveats section: backtest ≠ live performance

## Every Layer, Explained

### 1. Market Regime Filter
- Mechanic (the actual code)
- Why it matters / does the heavy lifting
- Investor critique: lagging, whipsaw risk, binary world

### 2. MRI Score (0–100)
- The 7 conditions with ACTUAL weights from the code (note the
  README is stale — 25/25/15/15/10/5/5 not 25/25/20/20/10)
- Golden Setup requirements
- Investor critique: weights are judgment calls, NaN handling
  is permissive

### 3. Signal Generator
- Hardcoded thresholds (MAX_POSITIONS=10, MIN_BUY_SCORE=75/85,
  MAX_SELL_SCORE=40, sector cap, ₹10Cr liquidity gate)
- The bear-regime auto-sell rule
- Investor critique: advisory not auto-execute, sector cap
  defaults to "UNKNOWN"

### 4. STEE Swing Execution Engine
- The 5 entry filters (close>10d high, vol>1.5x, top-30% close,
  gap<4%, candle ≤2xATR)
- Position sizing formula with ATR blend
- 4-tier exit ladder (hard stop → trail ratchet → 2R partial →
  EMA-10 trail)
- Investor critique: professional-grade money management,
  audit logging unusual for retail

### 5. QIF — The 7 Fundamental Agents
- Table of all 7 agents with their exact mechanics
- The receivables-vs-revenue red flag (channel stuffing)
- ROCE vs hardcoded 12% WACC
- Investor critique: 2 data points only, no sector-adjusted WACC,
  no real cash-flow statement

### 6. AI Forensic Debate Engine
- What GPT-4o-mini sees as input
- How the output gets translated into a ±20 numeric adjustment
  to PERX
- Investor critique: cheap model, narrative scraping reliability
  unproven, treat as tie-breaker not oracle

### 7. PERX / AAE Composite
- The actual formula: MRI×0.35 + QIF×0.40 + STEE×0.15 +
  Trajectory×0.10 + Debate_adj − Fragility×0.15
- Lifecycle classification (Institutional Expansion / Early
  Rerating / Euphoria / Accumulation / Distribution)
- Investor critique: only risk overlay is fragility, no macro,
  no sector normalization in PERX itself

## Cross-Cutting Strengths
(1) Disciplined layering (2) Realistic backtest construction
(3) Risk controls baked in (4) Defensive coding

## Cross-Cutting Weaknesses
(1) Regime lag (2) Hardcoded magic numbers, no sector adjust
(3) No short side (4) QIF is point-in-time not trajectory
(5) Capacity ceiling (6) AAE narrative layer is architecturally
ambitious but operationally unproven

## How I'd Actually Use This As A Small Investor
6 concrete steps (shadow mode 6mo → trust regime+score → QIF
as filter not trigger → AI debate as tie-breaker → manual STEE
→ skip PERX until validated)

## Source Files
(Exhaustive list of every .py / .md file referenced, so the
reader can verify any claim themselves)
```

## Verification Strategy
After writing the file:
1. `wc -w docs/investor/explain-investor.md` → confirm length in expected 2,500–3,500 range
2. `head -50 docs/investor/explain-investor.md` → confirm opening renders
3. `grep -c '^##' docs/investor/explain-investor.md` → confirm all 7 layer sections present (count of `##` headings should be ≥ 14: title + big-picture + headline + each layer section heading + critique sub-headings + cross-cutting + usage + sources)
4. `grep -E '(26\.39|10\.08|1\.48|75|MAX_POSITIONS|RISK_PER_TRADE_PCT|12%)' docs/investor/explain-investor.md` → spot-check that every quoted figure is present and correctly cited
5. Render check: ensure no broken Markdown (no unclosed code blocks, no orphan backticks)

## Decision Log
| Decision | Rationale | Rejected alternative |
|---|---|---|
| Single self-contained file | User asked for one file | Splitting into multiple per-layer files — overkill for a walkthrough |
| Use `docs/investor/` (per user choice) | Joins existing investor-focused docs | Project root (clutters) or `.kimchi/docs/` (not discoverable to humans) |
| Document README staleness (weights 25/25/15/15/10/5/5 vs documented 25/25/20/20/10) | Investor would otherwise be misled if they cross-check | Quietly match the README — would compromise honesty |
| Include source file references at the end | Lets investor verify any claim themselves | Inline citations — too noisy for prose |
| Lead with backtest then layers | The backtest is the headline; layers are the substance | Layer-by-layer first — buries the most compelling fact |

## Risks
| Risk | Likelihood | Mitigation |
|---|---|---|
| File is too long and the investor never reads it | Medium | Clear section headers, scannable tables, opening summary TL;DR |
| Code I've read gets updated and file goes stale | Medium | Cite file paths + line numbers so future readers can re-verify |
| Investor takes backtest numbers as live performance guarantee | High | Open with the backtest, but immediately follow with the caveats section |
| PERX layer described as more proven than it actually is | Medium | Explicitly mark AAE narrative layer as "architecturally ambitious but operationally unproven" |

<!-- PLAN_COMPLETE -->