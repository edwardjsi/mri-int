You've answered the one blocking question (Python, fit existing infra). All assumptions are resolved. The session is in plan mode — to unblock execution I present the finalized plan and emit the completion marker, which triggers your approval menu. Once you approve, permissions flip and I build immediately.

---

## Goal
Add a MOSI Lite scoring layer (pure-function Python engine) that enriches Breakout Radar candidates with a fundamental/structural score, decision score, confidence, and recommendation — displayed in the existing Breakout Radar UI and sorted by Decision Score — using only data already in the repo's tables.

## Constraints
- **No new dependencies.** Pure Python + existing psycopg2/pandas stack. No JS test runner added.
- **Pure functions, no UI logic in the engine.** Engine takes a stock dict, returns a dict. No DB I/O inside scorer functions.
- **No hardcoded stock names.** No duplicate business logic.
- **Backward compatible.** Existing `/api/breakout/radar` fields unchanged; new fields added alongside.
- **No API breaking changes.**
- **Swappable:** MOSI Lite → Full MOSI later without UI changes (UI renders only the output contract).
- **Missing fields handled gracefully** — a missing input scores 0 for that component, never raises.
- Follow AGENTS.md: update `Decisions.md`, `Sessions.md`, `Progress.md`; keep changes incremental & testable.
- RDS protection rules (Decision 026/027) untouched — no terraform, no destructive ops.

## Chunks

### Chunk 1 — Persist session onboarding (so future sessions don't re-burn tokens)
- **Scope:** `docs/AGENT_SESSION_BOOTSTRAP.md` (new), `docs/PRD_001_MOSI_LITE_ENGINE.md` (PRD copied from `~/Downloads`).
- **Depends On:** nothing.
- **Accept When:**
  1. `docs/PRD_001_MOSI_LITE_ENGINE.md` exists and matches the source PRD byte-for-byte (`diff` exits 0).
  2. `docs/AGENT_SESSION_BOOTSTRAP.md` exists and contains: architecture summary, data-availability audit table (PRD field → source table/column), and the MOSI Lite implementation plan.
- **Open Questions:** none.

### Chunk 2 — MOSI Lite scoring engine (pure functions + tests)
- **Scope:** `engine_mosi/__init__.py`, `engine_mosi/mosi_lite.py`, `engine_mosi/test_mosi_lite.py`.
- **Depends On:** Chunk 1 (docs established).
- **Accept When:**
  1. `python -m pytest engine_mosi/test_mosi_lite.py -v` passes with ≥7 tests covering: ROCE scoring, sales-growth scoring, debt scoring, missing-data handling, decision-score formula (0.6·MRI + 0.4·MOSI, clamped 0–100), confidence mapping by regime (Bull→HIGH, Sideways→MEDIUM, Bear→LOW), recommendation thresholds (≥90 TODAYS_PICK, 80–89 RESEARCH, 70–79 WATCHLIST, <70 IGNORE).
  2. `compute_mosi_lite` returns a dict with `mosiLiteScore` (0–100) and the max is provably 100 (M=20 + O=30 + S=30 + I=20) via a test with a perfect stock.
  3. A test with an empty `{}` stock returns `mosiLiteScore: 0` and a valid recommendation (`IGNORE`) without raising.
  4. `MARKET_REGIME` is a module-level configurable constant (default `"SIDEWAYS"`).
- **Open Questions:** none — scoring thresholds are pinned by the PRD verbatim.

### Chunk 3 — Backend: enrich `/api/breakout/radar` with MOSI Lite output
- **Scope:** `api/breakout_status.py` (modify the `/radar` query + post-process), `engine_mosi/mosi_lite.py` (already built in Chunk 2 — called from the API).
- **Depends On:** Chunk 2 (engine exists).
- **Accept When:**
  1. `GET /api/breakout/radar` returns each row with the existing fields PLUS: `mri_score`, `mosi_lite_score`, `decision_score`, `confidence`, `recommendation`.
  2. A row whose fundamentals are missing still returns valid values (scores 0, `recommendation: "IGNORE"`) — no 500.
  3. Existing frontend consumers that read `symbol`/`close`/`breakout_state` still work unchanged (`python -m py_compile api/breakout_status.py` clean; existing tests in `api/` still pass).
  4. Query uses parameterized inputs / `psycopg2.sql.Identifier` per repo hardening rules.
- **Open Questions:** none — the JOIN targets confirmed tables (`stock_scores`, `quality_verdicts`, `fundamental_financials`, `daily_prices`).

### Chunk 4 — Frontend: display 5 fields + sort by Decision Score
- **Scope:** `frontend/src/BreakoutRadar.tsx` (modify), `frontend/src/api.ts` only if type help needed (likely not — radar returns `any[]`).
- **Depends On:** Chunk 3 (backend returns new fields).
- **Accept When:**
  1. Each radar row shows: MRI Score, MOSI Lite Score, Decision Score, Confidence, Recommendation — per the PRD card layout.
  2. Rows are sorted by **Decision Score descending** (not MRI score). Broken-out / ready / consolidating grouping preserved within that order, OR grouping is replaced by Decision-Score sort — TBD by reading current UX (see Open Question).
  3. `cd frontend && npx tsc --noEmit` exits 0.
  4. `npm run build` succeeds.
- **Open Questions:** 
  - *Assumption:* keep the existing three-section grouping (Active Breakouts / Coiled Springs / Consolidating) but sort *within* each section by Decision Score. If you want a single flat list sorted by Decision Score instead, say so now. Default: preserve grouping, sort within. → Recording as assumption; will confirm in-build if it changes UX materially.

### Chunk 5 — Documentation & git
- **Scope:** `Decisions.md` (new Decision entry), `Sessions.md` (append session), `Progress.md` (append milestone). Commit on `feature/data-richness` (current branch).
- **Depends On:** Chunks 1–4 green.
- **Accept When:**
  1. New Decision entry logged (number TBD — next after 098) with rationale + status FINAL.
  2. `Sessions.md` has today's session entry with commits list.
  3. `Progress.md` updated.
  4. `git status` clean after commit; commit message ends with `Co-Authored-By: Kimchi <noreply@kimchi.dev>`.
- **Open Questions:** none.

## Verification Strategy
- **Unit:** `python -m pytest engine_mosi/test_mosi_lite.py -v` (Chunk 2).
- **Regression:** run the existing 47+ backend tests to prove no breakage: `python -m pytest engine_core engine_fundamental engine_debate api -q` (Chunk 3).
- **API:** `python -m py_compile api/breakout_status.py` + manual `curl http://localhost:8000/api/breakout/radar | jq '.[0] | keys'` shows the 5 new fields (Chunk 3).
- **Frontend:** `cd frontend && npx tsc --noEmit && npm run build` both clean (Chunk 4).
- **End-to-end manual:** load Breakout Radar in browser, confirm 5 fields render and rows are ordered by Decision Score (Chunk 4).

## Decision Log
1. **Impl language = Python, not TypeScript.** PRD says `services/mosiLite.ts`, but repo has no JS test runner and 47+ pytest tests. User explicitly chose "fit existing infra." Rejected: TS-only (adds vitest dep, splits test surface), TS+Python mirror (duplicates formula). Pure functions + swappable goal still met: UI renders only the output contract.
2. **Engine location = `engine_mosi/`** (new package, mirrors `engine_fundamental`/`engine_core`/`engine_debate` convention). Rejected: `engine_core/mosi_lite.py` (engine_core is regime/indicator territory; MOSI is a distinct scoring layer per PRD §Architecture).
3. **No new DB tables.** MOSI Lite is computed on read from `/api/breakout/radar` by joining existing tables. Rejected: persisting scores to a new `mosi_lite_scores` table — PRD says "configurable constant" for regime and treats this as a live filter, not a persisted artifact; persistence adds a migration + backfill with no stated benefit.
4. **Market regime = module constant** (default `"SIDEWAYS"`), per PRD ("can initially be a configurable constant"). Future auto-calculation is explicitly deferred by the PRD.
5. **PRD persisted to repo** (`docs/PRD_001_MOSI_LITE_ENGINE.md`) so future sessions don't lose it. + `docs/AGENT_SESSION_BOOTSTRAP.md` so tomorrow's session reads one file instead of re-spending tokens re-reading everything.

## Risks
| Risk | Likelihood | Mitigation |
|---|---|---|
| `quality_verdicts.agent_details` JSONB not populated for all radar symbols (Decision 098 backfill may be partial) | Medium | Scorer treats missing/`null` `agent_details` as 0 for that component — already in the missing-data handling. No crash. |
| 52-week-high not a first-class column (repo has `rolling_high_6m`) | Medium | Derive 52w high via `MAX(high) OVER (PARTITION BY symbol ORDER BY date ROWS 255 PRECEDING)` in the enriched query, or fall back to 6m high if 52w unavailable — scorer treats "near 52w high" as `close >= 0.97 * high_52w`. |
| Stage 2 trend has no explicit column | Medium | Derive from existing `ema_50 > ema_200 AND ema_200_slope_20 > 0` (Weinstein Stage 2 = uptrend above rising 200-MA) — both columns already in `daily_prices`. |
| Promoter holding column name differs across tables | Low | Confirmed `promoter_holding_pct` exists (schema line 469). |
| Frontend sort change breaks the three-section UX the user is used to | Low | Default to sorting *within* each existing section by Decision Score (assumption recorded in Chunk 4 Open Questions); revertible. |
| Session memory lost again next session | High (already happened) | Chunk 1 fixes this permanently via `docs/AGENT_SESSION_BOOTSTRAP.md`. |

<!-- PLAN_COMPLETE -->